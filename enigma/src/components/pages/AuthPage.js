import AuthForm from '../auth/AuthForm';
import Header from '../layout/Header';

const AuthPage = () => {
    return (
        <div>
            <Header />
            <AuthForm />
        </div>
    );
};

export default AuthPage;
